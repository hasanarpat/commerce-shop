export type TProduct = {
  id: string;
  title: string;
  description: string;
  image?: string;
  cost?: string;
  category?: string;
};

export type TCarouselSuggestion = {
  id: number;
  title: string;
  description: string;
  url: string;
  img: string;
};
